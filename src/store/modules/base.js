// import * as types from '../mutations-types'

// initial state
const state = {
  base: undefined
}

// getters
const getters = {
  // base: state => state.base
}

// actions
const actions = {

}

// mutations
const mutations = {
  /*
  [types.BASE_TYPE] (state) {

  }
  */
}

export default {
  state,
  getters,
  actions,
  mutations
}
